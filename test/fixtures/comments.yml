# == Schema Information
#
# Table name: comments
#
#  id         :bigint           not null, primary key
#  content    :text
#  user_id    :bigint           not null
#  post_id    :bigint           not null
#  created_at :datetime         not null
#  updated_at :datetime         not null
#


one:
  content: "Este es el primer comentario del primer post."
  post: one
  user: one


two:
  content: "Este es un comentario del segundo post."
  post: two
  user: two


three:
  content: "Este es otro comentario para el primer post."
  post: one
  user: one

